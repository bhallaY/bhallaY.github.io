[model]
name = Projects
label = Projects
hidden = yes
protected = yes

[fields.title]
label = Title
type = string

[children]
model = project
slug_format = {{ (this.pub_date|dateformat('YYYY/M/') if this.pub_date) ~ this._id}}
order_by = -pub_date, title

[pagination]
enabled = yes
per_page = 10
